# 🔐 CLI Генератор Паролей

Простой и безопасный генератор паролей для командной строки.  
Реализован для сравнения подходов **Python** и **Go**.

## 📋 Описание проекта

Это пет-проект, демонстрирующий реализацию одной и той же функциональности на разных языках программирования. Оба варианта предоставляют идентичный функционал и интерфейс командной строки.

## ✨ Возможности

- 🎲 Генерация криптографически стойких случайных паролей
- 📏 Настройка длины пароля (от 4 символов)
- 🔤 Гибкая настройка типов символов:
  - Строчные буквы (a-z)
  - Заглавные буквы (A-Z)
  - Цифры (0-9)
  - Специальные символы (!@#$%^&* и т.д.)
- 🎯 Предустановленные режимы сложности (простой/сложный)
- 📊 Генерация нескольких паролей одновременно
- 🚫 Опция исключения похожих символов (0, O, l, 1, I)
- 💻 Простой и понятный CLI интерфейс

## 🗂️ Структура проекта

```
cli-password-generator/
├── Python/
│   ├── password_generator.py    # Python реализация
│   └── README.md                
├── Go/
│   ├── main.go                  # Go реализация
│   ├── go.mod                    
│   └── README.md                 
└── README.md                     
```

## 🚀 Быстрый старт

### Python версия

```bash
cd Python
python password_generator.py -l 16
```

**Требования:** Python 3.6+

### Go версия

```bash
cd Go
go run main.go -l 16

# Или с предварительной сборкой
go build -o password-generator
./password-generator -l 16
```

**Требования:** Go 1.21+

## 📖 Примеры использования

### Базовые команды (одинаковые для обеих версий)

```bash
# Пароль по умолчанию (12 символов)
python password_generator.py
# или
go run main.go

# Пароль заданной длины
python password_generator.py -l 20
go run main.go -l 20

# Несколько паролей
python password_generator.py -l 16 -c 5
go run main.go -l 16 -c 5

# Простой пароль (без спецсимволов)
python password_generator.py -l 16 --no-special
go run main.go -l 16 -no-special

# Максимально сложный пароль
python password_generator.py --complex
go run main.go -complex

# Пароль без похожих символов
python password_generator.py -l 16 --exclude-ambiguous
go run main.go -l 16 -exclude-ambiguous
```

### Пример вывода

```
$ python password_generator.py -l 16
K8$mPq2#xR9@vL5n

$ go run main.go -l 12 -c 3

==================================================
  Сгенерировано 3 пароля (длина: 12)
==================================================

 1. aB3$xY9@mN2p
 2. pQ7#kL5!wR8z
 3. tM4$vX6@nJ9b
```

## ⚙️ Доступные параметры

| Параметр | Python | Go | Описание |
|----------|--------|-----|----------|
| Длина | `-l`, `--length` | `-l` | Длина пароля (по умолчанию: 12) |
| Количество | `-c`, `--count` | `-c` | Количество паролей (по умолчанию: 1) |
| Без заглавных | `--no-uppercase` | `-no-uppercase` | Не использовать заглавные буквы |
| Без цифр | `--no-digits` | `-no-digits` | Не использовать цифры |
| Без спецсимволов | `--no-special` | `-no-special` | Не использовать специальные символы |
| Исключить похожие | `--exclude-ambiguous` | `-exclude-ambiguous` | Исключить 0, O, l, 1, I |
| Простой режим | `-s`, `--simple` | `-s` | Только буквы и цифры |
| Сложный режим | `--complex` | `-complex` | Все символы, минимум 20 знаков |
| Справка | `-h`, `--help` | `-h` | Показать справку |

## 🔍 Сравнение реализаций

| Характеристика | Python | Go |
|----------------|--------|-----|
| Скорость выполнения | Средняя | Высокая |
| Размер бинарника | Интерпретатор (~20-30 MB) | ~2 MB |
| Зависимости | Нет (stdlib) | Нет (stdlib) |
| Кроссплатформенность | Да | Да |
| Простота установки | Требует Python | Компилируемый бинарник |
| Читаемость кода | Отличная | Хорошая |

## 🛡️ Безопасность

Обе реализации обеспечивают:
- Использование качественных генераторов случайных чисел
- Гарантию наличия символов каждого выбранного типа
- Случайное перемешивание символов в пароле
- Отсутствие предсказуемых паттернов

## 🎯 Цели проекта

1. Изучение реализации CLI утилит на разных языках
2. Сравнение подходов и идиом Python и Go
3. Практика работы с:
   - Парсингом аргументов командной строки
   - Генерацией случайных данных
   - Строковыми операциями
   - Структурированием кода

## 📝 TODO / Идеи для развития

- [ ] Добавить генерацию запоминающихся паролей (diceware)
- [ ] Добавить проверку силы пароля
- [ ] Сохранение паролей в файл
- [ ] Интерактивный режим
- [ ] Поддержка кастомных наборов символов
- [ ] Веб-версия с API
- [ ] Rust реализация для сравнения

## 📄 Лицензия

MIT

## 👨‍💻 Автор

[Александра Саганелидзе](https://github.com/alekarah)
