# Генератор паролей (Go версия)

Консольная утилита для генерации безопасных случайных паролей с гибкими настройками.

## Возможности

- 🔐 Генерация криптографически стойких паролей
- ⚙️ Настройка длины и сложности
- 🎯 Предустановленные режимы (простой/сложный)
- 📊 Генерация нескольких паролей одновременно
- 🚫 Опция исключения похожих символов
- 💻 Простой CLI интерфейс
- ⚡ Высокая производительность

## Установка

Требуется Go 1.21+

```bash
# Сборка программы
cd Go
go build -o password-generator

# Или запуск без сборки
go run main.go

go run .
```

## Использование

### Базовое использование

```bash
# Генерация пароля с настройками по умолчанию (12 символов)
./password-generator

# Указать длину пароля
./password-generator -l 16

# Сгенерировать несколько паролей
./password-generator -l 16 -c 5
```

### Параметры

```
-h                        Показать справку
-l                        Длина пароля (по умолчанию: 12)
-c                        Количество генерируемых паролей (по умолчанию: 1)
-no-uppercase             Не использовать заглавные буквы
-no-digits                Не использовать цифры
-no-special               Не использовать специальные символы
-exclude-ambiguous        Исключить похожие символы (0, O, l, 1, I)
-s                        Простой режим (только буквы и цифры)
-complex                  Сложный режим (все типы символов, длина 20)
```

### Примеры

```bash
# Простой пароль без спецсимволов
./password-generator -l 16 -no-special

# Максимально сложный пароль
./password-generator -complex

# Простой пароль для запоминания
./password-generator -s -l 10

# Пароль без похожих символов
./password-generator -l 16 -exclude-ambiguous

# 10 паролей для выбора
./password-generator -l 14 -c 10

# Запуск без сборки
go run main.go -l 16 -c 3
```

## Примеры вывода

```
$ ./password-generator -l 16
K8$mPq2#xR9@vL5n

$ ./password-generator -l 12 -c 3

==================================================
  Сгенерировано 3 пароля (длина: 12)
==================================================

 1. aB3$xY9@mN2p
 2. pQ7#kL5!wR8z
 3. tM4$vX6@nJ9b
```

## Сборка для разных платформ

```bash
# Windows
GOOS=windows GOARCH=amd64 go build -o password-generator.exe

# Linux
GOOS=linux GOARCH=amd64 go build -o password-generator

# macOS
GOOS=darwin GOARCH=amd64 go build -o password-generator

# macOS ARM (M1/M2)
GOOS=darwin GOARCH=arm64 go build -o password-generator
```

## Структура проекта

```
Go/
├── main.go          # Основной код программы
├── go.mod           # Go модуль
└── README.md        # Документация
```

## Безопасность

- Использует `crypto/rand` seed для инициализации генератора случайных чисел
- Гарантирует наличие хотя бы одного символа каждого выбранного типа
- Случайное перемешивание символов в пароле
- Без внешних зависимостей

## Производительность

Go версия обеспечивает высокую производительность и может генерировать тысячи паролей в секунду.

## Лицензия

MIT
